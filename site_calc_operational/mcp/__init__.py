"""MCP server package for site-calc-operational.

Exposes a FastMCP server that lets an LLM build operational device-planning
scenarios incrementally and submit them against a self-hosted ``server-onprem``.

Run with ``python -m site_calc_operational.mcp`` or via the
``site-calc-operational-mcp`` console script (requires the ``mcp`` extra).
"""
